{% embed '@DevsterCms/common/table/content_cell.html.twig' with {
    fit: field.isFit(),
    bold: cell.getBold(),
    title: title|default(null),
    alignment: cell.alignment.name,
    verticalAlignment: cell.verticalAlignment.name
} %}
    {% block content %}
        {{ cellHtml }}
    {% endblock %}
{% endembed %}